# 输出范例（对照用）

以下路径在 **`gd26-algorithm`** 仓库，生成新题时请保持相同版式与目录约定。

## 目录与命名

```
LeetCode/
└── LeetCode0102/
    ├── LeetCode0102-二叉树的层序遍历.md   ← 原题 Markdown
    ├── imgs/
    │   └── tree1.jpg                      ← 从官网下载
    └── 题解-LeetCode0102-广度优先.md       ← 题解（本技能不生成）
```

题号 `310` → 目录 `LeetCode0310`，文件 `LeetCode0310-最小高度树.md`。

## 题目索引（leetCode题目.md）

路径：`LeetCode/leetCode题目.md`。抓题或运行 `update_leetcode_index.py` 后，在 **`## 题目列表`** 中应出现对应链接：

```markdown
# LeetCode 题目索引

## 题目列表

- [102. 二叉树的层序遍历](LeetCode0102/LeetCode0102-二叉树的层序遍历.md)
- [542. 01矩阵](./LeetCode0542/LeetCode0542-01矩阵.md)
  - 常规解法为BFS，不难实现；
  - 但还有动态规划的解法，理解需要门槛
```

- **备注子项**（`  - ` 开头）：人工维护，索引同步时保留。
- **`## BFS相关扩展（待解题）` 等**：技能不修改，仅维护「题目列表」。

## 原题 Markdown 片段（102）

```markdown
# 102. 二叉树的层序遍历

**难度：** 中等

## 题目描述

给你二叉树的根节点 `root`，返回其节点值的 **层序遍历**。（即逐层地，从左到右访问所有节点）。

## 示例

### 示例 1
![示例 1 二叉树示意图](imgs/tree1.jpg)
- **输入：** `root = [3,9,20,null,null,15,7]`
- **输出：** `[[3],[9,20],[15,7]]`
```

## 原题 Markdown 片段（310，含多图）

```markdown
### 示例 1

![示例 1 树形示意图](imgs/example1.jpg)

- **输入：** `n = 4`，`edges = [[1,0],[1,2],[1,3]]`
- **输出：** `[1]`

**解释：** 如图所示，当以节点 `1` 为根时……
```

## 与脚本输出的差异

脚本生成的文件通常可直接使用；人工校对时常见增补：

- 示例图 `alt` 更具体（如「二叉树示意图」）。
- 示例下增加 **树结构简述**（非官网正文，需标注为自己总结时可放在示例小节末尾）。
- 独立成段的定义句（个别题在 HTML 中不在 `<p>` 内）并入 **题目描述**。

官方 **输入/输出/约束数值** 不得修改。
