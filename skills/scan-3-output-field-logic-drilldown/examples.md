# 字段计算逻辑下钻 — 示例与质量参照

思考顺序以 `SKILL.md` 中的 **「核心思路（三步走）」** 为准；本页补充成品参照与可复制骨架。

## 完整成品参照（仓库内）

以下文档可作为**篇幅、深度、图表密度、自检段**的参照成品（主题与项目相关，换题时保留结构即可）。**新产出默认落盘 `ai_docs/` + `YYMMDDNN-描述.md`**（与主技能及另两子技能一致）；下列路径为历史落盘示例：

- `cursor_docs/26041401-genTongAuthCode复杂字段L3L4计算逻辑下钻.md`（结构与质量参照；新文档请写入 `ai_docs/`）

阅读该文件时重点关注：

- 「总览：可合并讲解的字段族」表格（先合并再下钻）
- 分组标题下 **时序图 + 文字步骤 + 可选流程图** 的组合方式
- 「自检与合并说明」「不确定点」收尾方式
- 「引用源码位置」便于跳读的索引表

---

## 用户请求形态示例（触发本技能的典型说法）

- 「对基线文档里**复杂字段表**中 L3/L4 字段继续做**计算逻辑下钻**」
- 「这几个字段的**生成逻辑**要从代码里**梳理清楚**，按时序图 + 流程说明输出，并写 md」
- 「字段 A/B 的**多阶段覆盖**和**最终以哪张表为准**需要下钻」

---

## 迷你结构模板（可复制到产出物）

```markdown
# [主题] 字段计算逻辑下钻

> 基线：[路径或「无」]  
> 范围：[字段列表或 Lx]

**主路径**：[类#方法 → … 一句话]

## 总览：可合并讲解的字段族

| 分组 | 字段 | 共用核心 |
|------|------|----------|

## 分组 X：[字段族名称]

### 时序图

\`\`\`mermaid
sequenceDiagram
    ...
\`\`\`

### 计算流程（文字）

1. ...
2. ...

### 流程图（可选）

\`\`\`mermaid
flowchart TD
    ...
\`\`\`

## 自检与合并说明

### 可合并的讲解路径

- ...

### 不确定 / 待读者核对

1. ...
```

---

## 单字段图示策略示例

- **深链路**：父时序图只画模块边界；子节「核心选取策略」再画专用流程图（例如时间线分段 + 取第 k 条记录）。
- **多字段同源**：`field_a` / `field_b` 共用 `chooseRecord()`，只画一张「选取策略」流程图，两节文字各写「从选中记录取哪一列」。

---

## 质量自检清单（产出前勾选）

- [ ] 每个待下钻字段都在正文中有**明确计算/覆盖结论**或标明「非本链路写入」。
- [ ] 图表与文字**不矛盾**；分支与代码一致。
- [ ] 已做**字段族合并**，无大块重复段落。
- [ ] 收尾有**不确定点**，避免把推断写死成事实。
- [ ] 落盘文件名符合当日序号规则（若项目要求）。
